function showElement() {
  const element = document.querySelector('.element');

  // Thay đổi giá trị của opacity và transform để kích hoạt hiệu ứng
  element.style.opacity = '1';
  element.style.transform = 'translateY(0)';
}
